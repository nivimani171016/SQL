--CITY
--FIELD: ID, NAME, COUNTRYCODE, DISTRICT, POPULATION
--COUNTRY
--FIELD: CODE, NAME, CONTINENT, REGION, SURFACEAREA.....
--KEYMATCH : CODE = COUNTRYCODE

SELECT CITY.NAME
FROM CITY
JOIN COUNTRY
ON CITY.COUNTRYCODE = COUNTRY.CODE
WHERE COUNTRY.CONTINENT = 'Africa'
